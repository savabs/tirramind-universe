    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
B    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
2    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
—    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
K    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
A    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
C    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
I    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
K    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
/    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
;    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
C    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
I    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
K    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
Y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
M    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
B    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
O    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
L    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
C    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
H    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
A    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
N    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
G    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
C    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
I    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
K    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
+    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
A    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
U    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
L    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
R    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
O    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
O    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
V    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
N    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
C    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
O    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
L    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
U    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
M    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
N    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
>    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
`    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
`    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
`    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
`    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
>    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
`    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
`    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
`    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
`    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
B    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
#    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
C    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
I    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
K    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
z    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
{    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
}    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
{    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
}    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
{    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
}    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
{    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
Y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
M    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
B    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
O    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
L    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
C    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
H    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
A    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
N    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
G    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
}    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
~    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
{    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
L    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
I    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
R    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
O    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
M    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
M    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
A    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
P    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
}    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
~    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
{    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
L    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
I    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
}    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
!    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
{    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
N    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
A    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
M    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
C    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
H    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
A    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
N    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
G    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
}    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
!    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
&    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
&    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
{    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
X    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
C    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
H    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
A    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
N    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
G    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
C    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
H    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
A    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
N    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
G    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
}    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
\    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
V    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
N    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
C    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
O    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
L    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
U    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
M    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
N    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
8    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
V    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
N    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
C    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
O    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
L    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
U    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
M    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
N    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
8    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
*    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
A    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
U    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
L    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
R    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
O    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
O    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
>    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
C    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
z    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
1    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
V    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
N    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
C    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
O    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
L    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
U    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
M    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
N    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
V    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
N    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
C    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
O    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
L    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
U    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
M    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
N    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
#    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
U    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
P    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
C    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
R    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
M    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
O    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
V    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
A    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
L    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
1    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
5    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
0    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
0    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
#    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
>    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
A    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
*    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
`    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
`    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
`    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
`    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
>    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
U    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
P    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
C    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
R    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
M    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
O    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
V    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
A    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
L    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
;    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
*    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
`    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
`    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
`    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
`    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
L    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
I    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
R    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
O    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
M    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
M    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
A    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
P    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
;    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
'    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
'    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
8    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
5    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
%    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
*    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
`    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
`    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
`    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
`    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
N    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
A    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
M    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
C    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
H    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
A    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
N    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
G    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
/    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
j    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
q    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
L    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
I    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
R    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
O    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
M    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
M    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
A    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
P    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
z    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
>    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
U    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
P    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
C    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
R    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
M    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
O    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
V    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
A    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
L    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
q    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
L    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
I    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
R    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
O    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
M    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
M    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
A    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
P    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
q    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
L    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
I    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
{    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
}    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
&    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
>    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
#    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
C    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
I    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
K    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
*    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
*    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
+    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
/    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
6    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
0    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
#    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
C    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
#    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
I    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
G    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
P    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
>    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
U    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
N    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
I    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
C    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
W    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
I    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
N    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
q    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
>    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
C    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
W    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
N    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
Y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
#    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
q    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
L    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
I    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
R    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
O    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
M    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
M    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
A    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
P    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
L    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
I    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
Y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
M    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
B    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
O    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
L    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
C    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
H    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
A    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
N    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
G    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
{    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
}    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
j    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
j    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
j    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
j    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
!    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
j    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
j    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
j    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
j    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
j    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
>    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
6    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
0    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
&    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
<    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
6    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
0    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
j    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
j    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
j    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
Y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
M    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
B    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
O    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
L    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
C    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
H    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
A    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
N    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
G    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
j    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
j    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
&    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
j    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
&    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
j    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
!    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
j    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
X    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
C    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
H    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
A    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
N    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
G    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
R    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
A    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
N    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
R    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
j    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
^    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
z    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
0    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
9    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
q    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
N    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
A    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
M    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
C    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
H    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
A    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
N    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
G    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
>    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
L    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
I    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
R    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
O    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
M    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
M    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
A    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
P    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
*    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
*    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
q    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
L    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
I    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
S    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
R    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
O    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
M    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
M    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
A    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
P    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
&    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
~    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
&    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
q    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
*    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
E    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
A    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
U    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
L    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
R    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
O    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
O    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
>    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
D    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
F    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
-    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
0    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
`    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
`    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
`    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
`    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
R    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
;    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
`    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
`    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
`    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
`    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
q    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
q    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
T    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
p    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
m    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
{    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
:    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
}    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
(    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
)    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
.    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
d    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
[    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
,    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
s    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
o    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
]    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
=    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
w    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
y    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
b    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
k    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
x    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
c    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
h    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
a    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
g    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
_    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
f    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
i    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
l    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
"    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
t    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
u    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
r    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
n    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
     hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
e    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
v    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"

    hist = hist.sort_values("at_ts")
    hx = hist[["cik", "ticker", "exchange", "at_ts"]].rename(columns={"exchange": "ex_hist"})
    blank = ev.exchange.eq("")
    # Removals: the exchange it was on = latest capture at/before the step.
    # Listings: the exchange it joined = earliest capture at/after the step.
    for mask, col, direction in ((blank & ev.event.ne("LISTED"), "from_captured_at", "backward"),
                                 (blank & ev.event.eq("LISTED"), "to_captured_at", "forward")):
        sub = ev[mask].copy()
        if sub.empty:
            continue
        sub["ev_ts"] = pd.to_datetime(sub[col], utc=True)
        sub = sub.sort_values("ev_ts")
        got = pd.merge_asof(sub, hx, left_on="ev_ts", right_on="at_ts", by=["cik", "ticker"], direction=direction)
        got = got.set_index(sub.index)
        hit = got["ex_hist"].notna()
        ev.loc[got.index[hit], "exchange"] = got.loc[hit, "ex_hist"].values
        ev.loc[got.index[hit], "exchange_source"] = "wayback_exchange_file"
